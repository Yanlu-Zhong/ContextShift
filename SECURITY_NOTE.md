# Disclosure Note

Before making changes public, avoid committing private research artifacts such as manuscripts, anonymous-review files, real prompts/stimuli, model outputs, hidden-state dumps, scheduler logs, exact manuscript result tables, or manuscript-specific constants.

The `.gitignore` includes `data/private/` and `results/generated/` as guardrails, but researchers should still inspect staged files before every public push.
